## 1. Сохранение логина на странице входа

- [x] 1.1 Добавить в `src/main/resources/templates/login.html` обработчик события `submit` формы: сохранять значение поля ввода логина в `localStorage` под ключом `lastLogin` (обёрнуто в `try/catch`)
- [x] 1.2 Добавить восстановление сохранённого логина: при загрузке страницы (`DOMContentLoaded`) подставлять значение из `localStorage` (ключ `lastLogin`) в поле ввода, если оно непустое

## 2. Проверка

- [x] 2.1 Запустить приложение и проверить: после входа логин сохраняется в `localStorage`
- [x] 2.2 Проверить: при повторном открытии страницы `/login` сохранённый логин автоматически подставлен в поле ввода
- [x] 2.3 Проверить сценарий недоступного `localStorage`: страница продолжает работать без ошибок
